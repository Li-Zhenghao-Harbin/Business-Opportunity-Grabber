# 国网抓取流程对应的产品改进清单

## 1. 目标与范围

当前产品已经具备站点配置、公告列表抓取、本地去重、公告检索和按固定间隔定时抓取的基础能力。根据《招标数据抓取步骤（国网）》PDF，后续目标应从“采集公告列表”升级为“按项目持续归档、解析、比对并交付采购数据”的工作台。

本清单只定义要修改的内容，不包含代码实现方案或开发排期。

## 2. 现有能力与主要差距

| 能力项 | 当前状态 | 需要补齐的方向 |
| --- | --- | --- |
| 站点与列表抓取 | 已支持国网、南网及自定义站点 | 国网仅覆盖“招标公告及投标邀请书”一个公告栏目 |
| 公告基础字段 | 已保存标题、发布时间、编号、发布单位、截止时间等 | 缺少项目级、标包级、中标级和变更级的关联数据 |
| 本地存储 | 已保存公告与任务日志 | 缺少公告原文、附件、解析结果和导出文件的项目档案 |
| 定时任务 | 支持最短 5 分钟的固定间隔 | 缺少每日一次的增量策略、补抓策略和无更新处理 |
| 去重 | 已按公告内容生成去重键 | 无法识别同一项目的新增公告、变更公告和中标结果之间的关系 |
| 公告查看 | 可查看字段摘要并打开原文 | 缺少详情抓取、附件下载、解析状态和人工复核入口 |

## 3. P0：先形成“每日增量抓取 + 项目归档”闭环

### 3.1 国网公告栏目配置化

- 内置并可单独启停以下国网栏目：单一来源采购事前公示、年度采购计划预安排、资格预审公告、招标公告及投标邀请书、采购公告、推荐中标候选人公示、资质能力核实。
- 每个栏目保存独立的菜单标识、公告类型、抓取范围、是否下载附件、是否进入项目台账等规则。
- 站点配置页不再只配置一个入口 URL，应增加“公告栏目”管理和默认栏目模板。
- 抓取任务日志应按“站点 + 栏目”记录结果，明确新增、更新、跳过、失败数量。

### 3.2 每日增量与抓取水位

- 支持“每天固定时间执行”的计划任务，默认以最近 24 小时为增量窗口。
- 为每个栏目保存最后成功抓取时间、最后一条公告时间、最后一条公告标识，作为增量水位。
- 当本次没有新增或更新公告时，任务状态明确显示“无更新”，不创建无意义的公告记录或项目文件夹。
- 支持指定日期范围补抓，并避免补抓任务覆盖正常的增量水位。
- 支持分页抓取，直到达到时间边界或无下一页，避免只抓取列表首页。

### 3.3 公告详情与证据归档

- 对新增或更新公告进入详情页抓取，保存完整 HTML 快照及已清洗的正文文本。
- 按“发布日期 + 公告标题”生成项目归档目录；文件名需要清洗 Windows 非法字符，并保留可追溯的公告编号。
- 支持将公告详情导出为 PDF 或 DOCX；至少保留一种稳定、可离线查看的正文快照。
- 下载并保存公告附件，记录附件原始名称、来源 URL、下载时间、文件大小、哈希、下载状态和失败原因。
- 对重复下载、附件链接失效、文件损坏和权限受限提供可见的重试与人工处理状态。

### 3.4 可操作的处理状态

- 为公告和项目增加状态流转：新发现、详情已归档、附件已下载、待解析、解析完成、待人工复核、已确认、已取消、抓取失败。
- 在公告库中展示状态、最后更新时间和待处理原因，并支持按状态筛选。
- 记录每次自动处理和人工修订的时间、操作者、来源公告与变更说明，保证可回溯。

## 4. P1：把公告转成可用的项目和标包数据

### 4.1 项目主数据

- 建立“采购项目”实体，使用采购项目编号作为首选关联键，并以项目名称、采购人、批次、发布时间等作为辅助匹配条件。
- 公告详情页应提取并结构化保存：采购项目编号、采购项目名称、采购类型、招标/采购人、招标文件或采购文件获取截止时间、开标或开启应答文件时间、发布单位、公告类别、原文地址。
- 一条项目可关联多个公告、多个附件、多个标包和多个中标候选人公示。
- 对未能可靠匹配到已有项目的公告标记为“待关联”，由人工确认，不应静默合并。

### 4.2 附件识别、解压与解析

- 支持 ZIP、RAR（如运行环境许可）、PDF、DOCX、XLS/XLSX 等常见公告附件类型的识别和归档。
- 自动识别附件中名称包含“需求一览表”“货物清单”“分标”“分包”等关键词的文件或工作表；无法确认时进入人工选择队列。
- 对 Excel 附件支持单工作表和多工作表两种场景：多工作表先标注来源工作表为分标名称，再合并为统一明细。
- 解析结果必须保留原附件、原工作表、原行号或页码，方便从结构化数据回查原始证据。
- 对扫描 PDF、图片型 PDF、复杂合并单元格和非标准模板，应明确标记“需人工复核”，不能以空结果当作解析成功。

### 4.3 标包需求台账

- 建立“标包需求”明细实体，至少包含：分标名称、分标编号、包号、包名称、估算金额、数量、单位、物料/工程/服务描述、来源附件、来源工作表。
- 按“项目 + 分标编号 + 包号”形成唯一业务键；对于同一包号的多行物料，明确是汇总金额/数量还是保留明细，并同时保留可追溯明细。
- 将项目主数据自动带入每一条标包记录，包括项目编号、项目名称、采购类型、文件获取截止时间、开标/截标时间、招标人。
- 支持对金额、数量和单位进行格式规范化，保留原始值及标准化值，避免单位换算造成原始证据丢失。

### 4.4 可交付 Excel 台账

- 提供标准“招标及结果”导出，至少含项目主数据、标包需求、中标候选人、变更记录和数据来源五类字段。
- 默认导出项目级与标包级两个工作表；当业务需要一行一个标包时，将项目字段自动重复填充到所有标包行。
- 提供按日期、采购类型、发布单位、省份/区域、公告栏目、处理状态筛选导出。
- 导出文件应带生成时间、筛选条件和数据版本，不应覆盖既有人工维护的文件。

## 5. P1：处理变更公告和中标候选人公示

### 5.1 变更公告识别与比对

- 识别标题和详情中的“变更”“澄清”“补充”“终止”“取消招标”等关键词，并归类为变更公告。
- 将变更公告自动尝试关联至原项目和原标包；关联置信度不足时进入人工确认队列。
- 对“取消招标”将对应标包状态更新为“取消招标”，保留取消依据和来源公告。
- 对招标信息、金额、数量、时间等变更生成前后字段差异；界面中以醒目标记展示变更，导出时提供变更标识和变更前/后值。
- 变更不得直接覆盖原始数据；必须保留历史版本、变更时间和来源文件。

### 5.2 推荐中标候选人回填

- 采集推荐中标候选人公示详情和附件，提取分标、分包、中标候选人、中标金额、排序及公示时间。
- 按“项目 + 分标 + 包号”回填到已有标包需求，无法精确匹配时标记为待人工关联。
- 允许一个标包保存多名候选人及排序，避免只保留第一候选人。
- 在项目视图中提供“采购公告 -> 变更 -> 候选人公示”的时间线，展示当前最新业务状态。

## 6. P2：物资专用资质业绩要求

- 对采购类型为“物资”的公告自动识别“专用资质业绩要求”附件、章节或表格。
- 支持两种来源：独立附件直接归档；正文或附件内嵌内容解析后形成结构化条目。
- 将资质业绩要求按省份、项目、分标或物料类别归类，可直接导出为独立 Excel 工作表。
- 对提取不完整或来源不明确的条目保留原文摘录和定位信息，并进入人工复核。

## 7. 数据模型与本地存储调整

- 在现有公告记录之外，增加项目、附件、标包需求、变更记录、中标候选人、资质要求、处理记录和导出批次等数据对象。
- 明确关联关系：站点/栏目 -> 公告 -> 项目 -> 标包；公告 -> 附件；变更公告/候选人公示 -> 项目与标包。
- 将大文件及原始快照放入用户选择的归档目录，结构化索引保存在应用数据目录；不要把附件二进制内容直接塞入 JSON 数据文件。
- 支持配置归档根目录、保留策略、文件命名规则、下载并发数和最大文件大小。
- 数据库从单一 JSON 文件演进为适合关联查询与版本记录的本地数据库；原有 JSON 数据需要提供迁移与备份机制。

## 8. 界面与人工复核工作台

- 公告库升级为“公告、项目、标包、待处理、任务日志”五个核心视图。
- 项目详情汇总公告原文、附件清单、关键字段、标包明细、变更历史、中标候选人和导出记录。
- 增加待处理队列，集中展示下载失败、解析失败、字段缺失、关联冲突、变更待确认等事项。
- 为附件提供打开本地文件、打开来源链接、重新下载、重新解析、人工选择工作表/列映射等操作。
- 在字段人工修订时保留原始提取值与修订值，避免手工处理掩盖自动抓取结果。

## 9. 稳定性、质量与合规

- 每个栏目实现请求限速、可配置重试、超时、失败分类和断点续跑；避免单个附件失败导致整批任务失败。
- 为网站接口和页面结构建立适配器测试样本，覆盖列表、详情、附件、变更和中标候选人五类典型页面。
- 对关键字段建立完整性校验：项目编号、项目名称、公告时间、附件状态、标包编号、金额/数量等；缺失时生成明确问题，不将其伪装成成功数据。
- 对抓取、下载、解析和导出分别记录审计日志，保留来源 URL、时间、哈希和处理版本。
- 在使用前确认目标网站的访问频率、公开信息使用范围和附件下载规则；产品应支持限流并保留来源声明。

## 10. 建议的实施顺序与验收结果

1. 完成栏目配置、每日增量、分页、详情抓取、附件下载和项目归档。验收结果：新增公告可在本地形成完整、可回查的项目文件夹。
2. 完成项目主数据、标包附件解析和标准 Excel 导出。验收结果：一份标准采购公告可生成项目字段和可追溯的标包需求明细。
3. 完成变更比对和中标候选人回填。验收结果：取消、金额变更与候选人信息能关联回原标包，并保留历史版本。
4. 完成资质业绩提取、人工复核工作台、稳定性治理和数据迁移。验收结果：异常事项可集中处理，日常运行可审计、可恢复、可扩展。

## 11. 本轮不包含的内容

- 不修改现有抓取代码、前端代码或存储结构。
- 不立即承诺所有附件模板的完全自动识别；复杂 Excel、扫描 PDF 和非标准附件应保留人工复核路径。
- 不把南方电网适配直接等同于国网流程；南网应复用通用项目档案和附件处理能力，再按其栏目与页面结构单独配置适配规则。
